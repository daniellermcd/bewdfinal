<% if user_signed_in? %>
  <% if @albums.empty? %>
    Click the "New Album" button to start!
  <% else %>
    <h2>Your Albums:</h2>
    <ul>
      <% @albums.each do |album| %>
        <li><%= link_to album.title, album %></li>
      <% end %>
    </ul>
  <% end %>
<% end %>